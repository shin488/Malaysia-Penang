/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Malaysia-Penang",
  assetPrefix: "/Malaysia-Penang/",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
